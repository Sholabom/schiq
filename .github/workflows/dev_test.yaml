name: dev test workflow
on: [ push ]
jobs:
    test:
    runs-on: ubuntu-latest
    steps:
        -uses: actions/checkout@v2
        -run: echo "the job was automatically triggered a a ${{ github.event_name }} event."
        -run: python demo.py
      